#ifndef MENU_H
#define MENU_H

#include "Util.h"
#include "Menu.h"
#include "DynArray.h"
#include "Movie.h"
#include <iostream>
#include <string>
#include <sstream>
using namespace std;
class Menu
{
   public:
     void mainMenu(int* choice);
     void printMovieData(DynArray* arr);
     void printMoviesByGenre();
     void getMovieData(DynArray* arr);
     void getMovieName(string* n);
     string getInGenre();
   private:
};

#endif
